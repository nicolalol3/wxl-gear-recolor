# wxl-gear-recolor (WarcraftXL module)

Drop this folder under `wxl-core/scripts/` and rebuild WarcraftXL.

Pairs with the `WXLRecolor` addon (`/recolor`).

See the repository root README for install steps and limitations.
