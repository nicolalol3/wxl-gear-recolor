WXL RE — Section dispatch (evidence from logs + static RE)
==========================================================
Date: 2026-07-31

SECTION 8-9 = FACE/HAIR (not gear)
----------------------------------
Static: WXL-RE-COMPOSITE-FIELDS.txt, callers @ 0x4F0AF0
Runtime log (debug-615e3b, session 2690438xxx):

  face_gear_native section 8:
    plate_pvppaladin_b_04_bracer_al_U.blp
    Leather_Horde_A_02Brown_Pant_LU_U.blp
    plate_pvpdeathknight_b_04_belt_lu_U.blp

Vanilla PasteFromSkin path pastes item TextureComponents onto section 8
when gear rebuild runs — corrupts face mip (black/wrong face on self).

Fix: face_gear_block — return without native paste for IsItemComponentTexture
on sections 8-9. Character face/scalp paths (face_skin_ok) still allowed.

SECTIONS 0-7 = GEAR
-------------------
dst = *[0x00B6B870] shared scratch
RenderPrepSections reads [edi+0x0C] dirty mask, dispatch @ 0x00B6B88C

LOCAL SELF vs REMOTE
--------------------
Logs: face_skin_ok only ownerLow=0 (outside tint session) at char select.
In-world local RenderPrep: stem_tint sections 0-7 only, zero skin_layout.

local_native_first must run before first gear tint (same as remote).
pendingEnterWorld forces nativeDone=false on enter world.

NEXT RE
-------
Log dirtyMask in RenderPrep (added: ReProbe pasteStrictOk field now dirty&0x3FF)
Confirm bit 8-9 clear during gear-only rebuild.
